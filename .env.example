# Copy to .env and fill in local paths. All experiment code reads paths from here.
POPLLM_ROOT=/path/to/popllm
MODELS_ROOT=/path/to/models
HF_CACHE=/path/to/hf_cache
HF_HOME=/path/to/hf_cache
DATASETS_ROOT=/path/to/hf_datasets_hub
EXP_ROOT=/path/to/experiment_outputs
